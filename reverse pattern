#include<stdio.h>
int main()
{
	int n,i,j,k;
	printf("enter the number:");
	scanf("%d",&n);
	k=n;
	for(i=1;i<=6;i++)
	{
		for(j=1;j<=i;j++)
		{
			printf("%d ",k);
			k--;
		}
		printf("\n");
	k=n;	
	}
}
